#include "français.h"
#include "stdio.h"

commentaire(« La première fonction du programme »)
entier début(entier compte_arguments, caractère pointeur pointeur arguments) bloc(
    imprimer_avec_format_de_texte(texte(«, Bonjour Monde! ,») nouvelle_ligne);

    retour zéro;
)
